import { logRoles } from "@testing-library/react";
import React from "react";
function Logout(){
    return(
        <div>
            <h1>logout page</h1>
        </div>
    );
}
export default Logout;